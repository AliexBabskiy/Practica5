<Window x:Class="PePract5.WindowKassa"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PePract5"
        mc:Ignorable="d"
        Title="WindowKassa" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button x:Name="BtCheckk" Content="Чек" Click="Checkk_Cl"/>
        <Button x:Name="BtCheckk_Histori" Content="История Чеков" Grid.Column="1" Click="Checkk_Histori_Cl"/>
        <Button x:Name="BtClient" Content="Клиент" Grid.Column="2" Click="Client_Click"/>

        <Frame x:Name="FrameP" Grid.Row="1" Grid.ColumnSpan="6" NavigationUIVisibility="Hidden" Grid.RowSpan="5"/>
    </Grid>
</Window>
